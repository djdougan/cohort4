import WorkingWithArrays from './WorkingWithArrays.js';


test("Test: clear(array) returns []", () => {
    expect(WorkingWithArrays.clear()).toEqual([]);
    expect(WorkingWithArrays.clear()).toEqual([]);
    expect(WorkingWithArrays.clear()).toEqual([]);
});

test("Test: clear(array) returns []")